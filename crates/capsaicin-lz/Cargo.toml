[package]
name = "capsaicin-lz"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure-Rust decoder for SPICE LZ compressed images. Standalone — no SPICE wire-format dependencies."

[dependencies]
thiserror = { workspace = true }
